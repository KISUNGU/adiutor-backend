# Configuration Nixpacks pour Railway

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci --legacy-peer-deps"]

[phases.build]
cmds = ["echo 'No build step required'"]

[start]
cmd = "node server.js"
